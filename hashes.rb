#!/usr/bin/env ruby

# Hash
# An unordered, object-indexed collection of objects(or key-value pairs)

car = { 'brand' => 'Ford', 'model' => 'Mustang'}

# return value for a key in a hash
puts car['model']

# returns all the keys as an array
car.keys

# returns all the values as an array
car.values